{
  "name": "landing-rest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/main.js",
    "dev": "node --watch src/main.js",
    "lambda:install": "npm install --prefix lambda --omit=dev",
    "sam:build": "sam build",
    "sam:deploy": "sam deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "contentful": "^11.12.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "serverless-http": "^3.2.0"
  }
}
